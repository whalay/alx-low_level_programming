#include "main.h"
/**
 * 24_hours - check fr last number
 * 
 * Return: success aways
 */

void jack_bauer(void)
{
	for (a = 48; a <= 50; a++)
	{
		for (b = 48; b <= 57; b++)
		{
			for (c = 48; c <= 53; c++)
			{
				for (d = 48; d <= 52; d++)
				{
					if (a >= 50 && b >= 52)
					{
						_putchar(a);
						_putchar(b);
						_putchar(58);
						_putchar(c);
						_putchar(d);
						_putchar('\n');
					}
				}
			}
		}
	}















	
