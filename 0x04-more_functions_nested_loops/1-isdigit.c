#include "main.h"
#include <stdio.h>

/***
 * _isdigit - Check if a char is a digit
 *
 * @c: must be a number
 *
 * Return: 1 0r 0
 */

int _isdigit(int c);
{
	if ((c >= 0) && (c <= 9))
	{
		return (1);
	}
	else
	{
		return (1);
	}

}
